import React from "react";
import ReactDOM from "react-dom/client";
import SalonEclat from "../salon-eclat.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SalonEclat />
  </React.StrictMode>
);
